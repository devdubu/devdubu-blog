---
sticker: emoji//1f4d7

slug: "연구소-회의"
---
# 10/4 회의
Pre-define-Task
Pipe -> Buffer

직렬 Buffer 문제를 Memory 적재 방식으로 해결
—> 직렬 방식 대용량 데이터 재생성 문제는 현재 해결해야할 문제는 아님
	동시성 문제는 다른 가능한 방법을 고민해보기
Stateless에 대한 이야기
Memory Access -> Redis에 대한 사용?

Service -> 사용자 응답
Task -> Service에서 수행하는 과정
Pipe -> 데이터 변환

Task - Pipe - Task


# 10/10 회의

# 10/17일 회의
## Frontend
- layout
- colume, vertical layout
- json -> react, vue -> 출력?

- 표준웹 - devextreme 사용
- 1,4분기에 베타를 출시 -> devextreme 제거

# 10/20 - Backend
- meta module - json
- 처음부터 너무 타이트하게 잡아 놓게 되면 추후에 확장성이 문제가 된다.
- AWS에서의 Json을 예를 들면 된다.
	- 단순한 Key 값, 많은 예약어를 중심으로 진행해 나아가야 할 것 같다.
	- 

# 10/23일
## FrontEnd
real grid는 Javascript 베이스로 되어있다.
-> Front 플랫폼 사항에서는 오히려 잘 된 일?
cell 단위의 layout 지정
뒤에서 나오는 이슈는 모든 것들을 다 고려 해야 한다.
- 당연히 하위 호환성을 생각해야 함
- 많은 가짓수를 생각 해야 한다는 뜻


# 10/24일
## FrontEnd
- layout
	- location
		- grid

### 컨테이너
- layout_type : tab, colume, row
- width
- height
- item
- style
- class
- scroll
- margin
- padding
### 컴포넌트
- type - input_type
- width
- height
- ajaxURL
- item
	- combo
	- radio
	- checkbox
	- date
- grid
	- type
	- colume
	- footer
	- header
	- 개인화
	- size
	- dataset
	- excel
		- 엑셀 다운로드 파일명
	- select
		- colume 
		- row
		- cell을 클릭했을 때 하는 방식
			- 
	- 
# 10 / 25
## Backend
task의 목적
- DB
	- DB dataget
	- DB data update
- File ReadWrite
	- Temp 파일 저장
		- 그대로 저장
		- modify 저장
	- DB 저장
- API - slack
- RFC

### DB
- 주로 data read/write...
- 


### File Upload
- Task의 입장
#### Service
```json
{
	"sid" : " ",
	"params" = ?,
	process = [
		
	]
	
}
```

#### task
```json
{
	"id" : " 테스크 명 ",
	type: "task",
	adapterid = " ",
}
```

#### Adapter
```json
{
	id: "",
	
}
```

# 10 / 30
- task의 target이 바뀌더라도,해당 task가 만들어지지 않아야함
- 구현체를 바꾸지 않고,  : data read
- 

# 3/4
외부 개발자가 작성하는 소스 코드 -> demo
우리가 개발해야하는 소스 코드 -> nuxt
	nuxt 상속 받는 개념
nuxt와 상관 없는 js, ts 로직 들어가는 곳 -> core


특정 디렉토리를 상속하는 구조가 있다.

```vue
<script>
definePageMeta({
	middleware: [‘h-auth’]
})
</script>
```

## 문제점
- 우리가 만든 컴포넌트를 모른다
- 각각의 프로젝트 사이트에서 실행되기에, 상대 경로가 이 안에 없다면, 경로를 찾을 수 없다.
	- 리소스 (아이콘, 이미지)
- 외부와 우리의 명확한 분리
	- Naming Rule?

- npm 설치 -> import auto를 빠지게 한 후, import auto 로 넣을 것과 수동으로 제어할 것
- 








