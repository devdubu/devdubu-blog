---
sticker: emoji//1f4c6
종료 날짜: 2025-02-13
시작 날짜: 2025-02-13
분류: 출장 - HD Electric
카테고리:
  - Application
특징:
  - Front
  - Vue3
---
# 할일
 - SERNO로 오는 값은 serialNumber 3자리와 test Id 한자리 이다.
 - 이를 나누고 난 후에 해당 selectbox에 저장 하면 된다.
- 정규식 로직을 넣어서 숫자 이외는 못받게 하는 로직을 넣어야함
	- 이 부분은 input-number가 해결 될때까지는 해당 사항은 보류
- 


# 온도 상승
API call
- `searchPjt`
	- 해당 값으로 처음 api call을 해줌
	- 해당 값이 몇개가 되던 0번째를 박아줌
	- 그리고 만약 이 데이터 값이 없다면, 아래의 api 를 call함
- `searchPjtStd`
	- 해당 값은 위의 api 데이터가 없을 시에, 기본적 데이터만 박아 넣는 api임
	- 이때 serial no 는 '001'로, testId는 1로 셋팅해서 api 를 call 함
- `searchData
	- 해당 값은 실제로 값이 있다면, call 함