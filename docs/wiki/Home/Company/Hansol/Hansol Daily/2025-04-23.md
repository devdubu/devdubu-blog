---
sticker: emoji//1f4c6
종료 날짜: 2025-04-23
시작 날짜: 2025-04-23
분류: 노트
카테고리:
  - Application
---
# UI 분리 작업
## Core 분리 작업
- UI 로직에서는 단독으로만 사용 가능하게만 처리
- 비즈니스 로직 단을 없애기


`vite-plugin-dts`

- 해당 라이브러리는 `components.d.ts`를 자동으로 만들어주는 plugin이다.
- 해당 plugin을 적용시에는 자동 완성 기능을 보장해준다.

```js
import ParsiXTemplate from 'parsix-template';

app.use(ParsiXTemplate); // ✅ 올바른 방식
```

```js
// index.ts
import PxAdminLayout from './components/PxAdminLayout.vue';

export const ParsiXTemplate: Plugin = {
  install(app: App) {
    app.component("PxAdminLayout", PxAdminLayout);
  },
};

export { PxAdminLayout }; // 💡 개별 컴포넌트도 export

export default ParsiXTemplate;
```

위 두개의 차이점은 import를 받을 때 객체처럼 받냐 아니냐의 차이점이다.

```json
{
	"exports": {
    ".": {
      "import": "./dist/index.es.js",
      "require": "./dist/index.cjs.js",
      // 해당 css 파일이 기본적으로 포함되어 나와야할 경우
      "style": "dist/hui.css"
    },
    // Styple 파일을 직접 import 할일이 존재할 경우
    "./dist/hui.css": "./dist/hui.css"
  },
  "types": "dist/index.d.ts",
  // import 'hui' 를 했을 때 기본적인 style 경로 지정
  "style": "dist/hui.css" ,
}
```