---
시작 날짜: 2025-07-14
종료 날짜: 2025-07-14
분류: 노트
카테고리:
  - Application
특징:
  - Nuxt3
sticker: emoji//1f4c6
---
# 할일

- [ ] `/api/admin/code` 인지 `/api/code` 인지 확인해보기
	- [ ] `/api/admin/code` 일 때는 `initData()` 에 로직이 들어가야하고
	- [ ] `/api/code` 일때는 `initDataMin()` 에 로직을 배치하면 된다

```js
export const getCommonCodes = async (codeGroupCd: string) => {
	return await mapPost('/api/code/list', { CODE_GRP_CD: codeGroupCd })
}
```

현재 코드를 기반으로 구성하면 codeGroupCd를 필수적으로 받아야하는데, 해당 Cd 값을 받지 않으면, 모두 뱉는건가?
