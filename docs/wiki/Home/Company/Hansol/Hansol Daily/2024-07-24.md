---
sticker: emoji//1f4c6
종료 날짜: 2024-07-24
시작 날짜: 2024-07-24
분류: 노트
카테고리:
  - Application
특징:
  - Nuxt3
  - Front
---
# 할일

- 만약 값이 없을 때는 어떻게 팝업창을 띄울건가 스밧..
    - 기본 값을 채워서 하려고 했더니, 그건 안된다.
    - 결국은 oneClick시에 editor에서 해당 속성을 걸러낸 후에 popup을 띄워야함
- 백엔드에 샘플로 `USER_NAME` 과 `USER_ID` 를 받아오기
    - 정상적으로 잘 덮어쓰는지 테스트
- CSS 개선

# Error

:::error Blocked aria-hidden on a `<input>` element because the element that just received focus must not be hidden from assistive technology users. Avoid using aria-hidden on a focused element or its ancestor. Consider using the inert attribute instead, which will also prevent focus. For more details, see the aria-hidden section of the WAI-ARIA specification at [https://w3c.github.io/aria/#aria-hidden](https://w3c.github.io/aria/#aria-hidden).


:::
