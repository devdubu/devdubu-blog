---
sticker: emoji//1f4c6
종료 날짜: 2024-06-24
시작 날짜: 2024-06-24
분류: 노트
카테고리:
  - Application
특징:
  - Front
---
# 할일

- 클린코드에 대한 규칙 확인 해보기

|접두사|의미|
|---|---|
|RealGrid|리얼 그리드|
|init|처음 값(함수 등)을 셋팅할 때|
|Config|어떤 값에 대한 설정 및 셋팅?|
|get|함수의 return 값이 어떤 접미사의 값을 뱉어줄 때|
|`_`|같은 이름으로 쓰이는 변수들의 명명들이 겹칠 때 지역 변수로 앞에 접두사를 붙인다.|
|call|API를 통해서 해당 데이터를 호출하는 함수 접두사로 쓰인다.|
|||

## HGrid

- Grid에서 Updated가 된 값인가? 아닌가에 대한 판단은 어떻게 하지?

```tsx
gridView.onEditCommit = function(){
	...
}
```

위 수준으로 해결

문제는 `updatedRow` 임

values에서 차례대로 해당 값에 대한 내용을 작성해드려야 함

- ~~hcore/ui/grid/index.ts - saveRows ⇒ getCURData 로 명명 변경~~
- ~~행추가 시 최상위 row로 추가~~
- ~~버튼 명명 변경~~
- 행 복사

```tsx
onMounted(async () => {
    const propsColumns = confProps.columns
    addEventsOnGrid()
    const realGridColumnsInfo = await convertToRealGridColumnsInfo(propsColumns)
    // addEventsOnGrid() -> 안됨
    initRealGrid(gridTargetDiv.value, realGridColumnsInfo)
    // addEventsOnGrid()

    if (gridAutoLoad) {
        const propsData = confProps?.data
        await loadInitialGridData(gridApi?.read, propsData)
    }
})
```

위 코드를 보게 된다면, `addEventsOnGrid()` 쪽을 `GridColumesInfo()` 셋팅보다 상단으로 올리니까 API가 된다.

왜지 시댕

이유는 우선 나중에 찾아봐야겠다.

`GridColumesInfo`바로 밑에 부터는 싹 다 안된다.

데이터를 먼저 로드를 한 후에 colume을 셋팅 해야 하는 건가?