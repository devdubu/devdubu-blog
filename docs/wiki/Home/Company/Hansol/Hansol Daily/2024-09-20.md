---
sticker: emoji//1f4c6
종료 날짜: 2024-09-20
시작 날짜: 2024-09-20
---
# 할일

## Never의 의미

자꾸 never가 나를 방해한다.

개 열받네

## setEditable 추가 작업

- row 데이터 전달
    - 매개변수 추가로 하여 row 데이터 전달

### Checkbox

- 동적 readonly 적용 - 완
- readonly 적용 - 완

### codebox

- 동적 readonly 적용 - 안됨
    - 함수를 이용해서 동적으로 컨트롤이 불가능함
    - 이 부분은 `gridCellEventConfig` 에서 `gridCellConfig` 의 코드를 로직상을 분리를 시키던 로직을 변경하던 해야함
    - 이유인 즉슨, column을 첫 셋팅할 때는 당연히 default로 readonly가 false인데, stylecallback을 통해 바꿀려니 싱크가 안맞는 문제다.
    - 싱크를 좀 맞추려면 model 매개변수로**만 판단하기엔 너무 부족하다**
    - 결국 동적으로 바꾸려면, 동적으로 바꿔야하는 결과값과 model 의 매개변수를 동시에 비교해야한다.
- readonly 적용 - 완