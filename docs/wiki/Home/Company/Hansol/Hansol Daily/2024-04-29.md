---
sticker: emoji//1f4c6
종료 날짜: 2024-04-29
시작 날짜: 2024-04-29
분류: 노트
카테고리:
  - Application
특징:
  - Front
---
# Toast UI 개발

props로 전달하려고 하니, 반응성을 잃는 이슈가 발생했다.
방법은 2가지로 압축 할 수 있다.
**props를 getter 함수로 선언하기**
**toRef 사용하기**

## STDWEB V3
DB 데이터 필수
UI path : 확장자 파일까지 전부
URL Path : `/hpage/[pcd]`

```jsx
const view = {
    name: 'admin-code-list',
    prgmCd: pcd,
    title: '공통코드 관리',
}

const func = {
}

export { view, func }
```

기본 셋팅 → `sample/configUi/list.js`
기본 필수 셋팅값

```jsx
interface container{
	topbar : itmes,
	main : items,
	bottombar : items,
	row: items,
}
```

## 최상단 type

- container
- form

## 2depth

- topbar
- bottombar
- row
- searchform

모든 child는 items로 되어 있고, items = [\{}]

button에서의 icon → css 전용 / fontIcon → js config 설정에서 사용 으로 구분

button의 event는 객체를 `event : { }`

### search form

- 접기 펼치기 기능
- form 관련 기능 → submit의 역할을 하는 form으로서 재정의
- name 필
- `searchTargetName` 검색을 할 대상에 대한 `name`

### form

- search 조회 / save / delete
- 디자인

## store
- pcd를 기준으로 name으로 key로 사용 → 필수 요소

seelctbox → api

```jsx
api: {
	url: "",
	params: ""
}
	
```

---

#Nuxt3 #Vue3 