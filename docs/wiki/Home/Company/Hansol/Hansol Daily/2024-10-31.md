---
sticker: emoji//1f4c6
종료 날짜: 2024-10-31
시작 날짜: 2024-10-31
분류: 노트
카테고리:
  - Application
특징:
  - Front
  - Nuxt3
---
# Validation

## Validation Required

## check configuration

```tsx
{
  type: 'grid',
  validate: 'checkGridValidation'
}
....
const checkGridValidation = (editedRowData) => {
  if(check) return true
  else return true
}
```

- Grid 전체 적인 타입에서 제공해주는 config

## Validation 상태 알림 로직

validation 결과가 false일 경우 “저장” 진행을 멈추고 문제가 되는 validation data에 대한 정보를 노티한다.

- 어떤 column의 몇번째 row에서 문제가 있었는지 알림을 띄워준다.
    - 모든 문제를 한꺼번에 보여줄 것인가? 한개씩 보여줄 것인가?
- 첫 번째 문제가 되는 행으로 row focus를 이동 시켜 주면 좋다.

## Validation API 추가

```tsx
hpage().getGrid().isValid()
```

위 API 추가

---

Validation 값의 범위 지정이 column단위?

`editedRowData` 에 어떤 값을 넣을 것인 지

- `editedRowData` 에 fieldName과 value ?
- Save는 Action Btn 기준? - O
- cud 보내는 데이터만 validation 의 대상이 됨
    - validation에서 줄 때 변경된 데이터는 모두 array로 처리
    

# README

Nuxt Content 걷기?

README 문서 버전 명시

## 테스트