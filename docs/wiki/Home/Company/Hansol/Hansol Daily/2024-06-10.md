---
sticker: emoji//1f4c6
종료 날짜: 2024-06-10
시작 날짜: 2024-06-10
분류: 노트
카테고리:
  - Application
특징:
  - Front
---
# RealGrid

테스트를 진행하기 위해서 demo data를 넣으려고 하니, pcd가 없다는 오류가 뜸
api통신이 안되다 보니 pcd가 없는 것 같음
mock서버에서 해야 할 지 아니면, 백엔드를 요청 해야 할지는 고민해봐야 할 부분
pcd 데이터를 store 적재 하는 부분이 결국은 `view = { }` , `func = { }` 쪽인듯

## store에 수동으로 접근

우선 gird가 작동 되려면, sample로 store를 수동으로 접근해서 기타 함수들을 등록할 수 있어야 함
그래야지 pcd 키 값이 해당 store에 등록 되니까 찾는 것도 가능해 짐

# RealGrid

- 특정 Index로 Grid추출은 `getRows` 를 하면 된다.
- 수정 사항에 대한 부분은 만약 type 문제가 해결 된다면, update된 상태의 행 index 값을 받아오는 걸로 처리 하면 된다.
- `getRows()` 와 같이 row 데이터 추출을 여러 row 데이터를 추출하려면(선택적) 방법이 없는지 찾아 볼것
    - `( )` 안 매개변수가 배열로 들어가는지,,,안들어가면 forEach 써야하는데 싫은데